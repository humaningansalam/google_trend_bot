version:  '3'
services:
  app:
      build: .
      restart: unless-stopped
      env_file:
        - .env
      ports:
        - 8182:8000
      logging:
        driver: "json-file"
        options:
          max-size: "2m"
          max-file: "5"
